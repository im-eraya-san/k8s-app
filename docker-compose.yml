services:
 service1:
  image: 3raya/sql-web-admin:0.1
  container_name: postgres-admin
  network_mode: host
  restart: always
  environment:
    - HOST=192.168.1.4
    - DB_PASSWORD=pg123
    - DB_USER=postgres
    - DB_NAME=postgres
